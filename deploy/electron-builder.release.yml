appId: com.openweave.desktop
productName: OpenWeave
asar: false
directories:
  output: deploy/target/default
files:
  - dist/**/*
  - package.json
extraResources:
  - from: bin
    to: bin
mac:
  identity: null
  category: public.app-category.developer-tools
  artifactName: "${productName}-${version}-${arch}.${ext}"
linux:
  category: Development
  artifactName: "${productName}-${version}-${arch}.${ext}"
win:
  artifactName: "${productName}-${version}-${arch}.${ext}"
  signAndEditExecutable: false
nsis:
  oneClick: false
  perMachine: false
